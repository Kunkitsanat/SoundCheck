name: sound_check
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  record: ^7.1.1       # รองรับ Android, Web, Linux
  audioplayers: ^6.0.0 # สำหรับเล่นเสียงที่อัดไว้
  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
